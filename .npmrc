# Allow postinstall scripts for build-tool packages that need them.
# This suppresses npm's "install-scripts blocked" warnings for trusted
# native-binary dependencies such as @swc/core and esbuild.
allow-scripts=true
